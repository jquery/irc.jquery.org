[11:49:38] <gnarf> hey FYI ryanneufeld danheberden: if you ever do pacakge updates on the server with jenkins, beware --- it's init.d script doesn't seem to actually kill it sometimes, requires a kill -9 or you'll have two jenkins running which breaks everything
