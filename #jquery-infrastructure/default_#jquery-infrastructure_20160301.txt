[15:29:22] <s5fs> boop
[15:38:00] <s5fs> Notice! ZNC server is a bit tight for resources so I'm going to double the memory this evening. Unfortunately, this requires a reboot, so expect 5 minutes or so of downtime at some point later today.
